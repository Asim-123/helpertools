import { metadata as toolMetadata } from './metadata';

export const metadata = toolMetadata;

export default function PageSpeedCheckerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
